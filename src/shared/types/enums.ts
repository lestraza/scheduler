export enum IncrementDecrement {
  Inc = "cnc",
  Dec = "Dec",
}

export enum EventVariant {
  Event = "primary",
  Task = "secondary",
  Holiday = "success",
}

export enum EventType {
  Event = "event",
  Task = "task",
  Holiday = "holiday",
}
